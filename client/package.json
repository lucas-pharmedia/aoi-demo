{
  "name": "aoi-client",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:bench": "vite --open /bench.html",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "deploy": "npm run build && aws s3 sync ./dist s3://mmo-test-frontend --region ap-east-2 --exclude \"index.html\" --delete && aws s3 cp ./dist/index.html s3://mmo-test-frontend/index.html --region ap-east-2 --cache-control \"no-store, no-cache, must-revalidate, max-age=0\" --expires \"0\""
  },
  "dependencies": {
    "phaser": "^3.87.0",
    "phaser-navmesh": "^2.3.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.0.0"
  }
}
